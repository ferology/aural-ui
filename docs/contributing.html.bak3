<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contributing - Aural UI</title>

    <!-- Aural UI CSS -->
    <link rel="stylesheet" href="./aural-ui.css?v=9">

    <!-- Choose theme -->
    <link rel="stylesheet" href="./dark.css" id="theme-link">

    <!-- Lucide Icons (MIT License) -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        body {
            display: flex;
            margin: 0;
            padding: 0;
        }

        /* Sidebar Navigation */
        

        

        

        

        

        

        
        
        
        
        
        
        
        

        
            50% { transform: scaleY(1); opacity: 1; }
        }

        

        

        

        
            50% { transform: scale(1.3); opacity: 0.7; }
        }

        
            50% { transform: scale(0.9); opacity: 0.5; }
        }

        

        

        

        

        

        

        

        

        

        

        

        

        

        

        /* Main Content */
        .main-content {
            
            flex: 1;
            padding: var(--space-8);
            max-width: calc(1400px + 280px);
        }

        /* Section Styles */
        .section {
            margin-bottom: var(--space-16);
            scroll-margin-top: var(--space-8);
        }

        .section-title {
            font-size: var(--text-3xl);
            font-weight: var(--font-bold);
            color: var(--color-text-primary);
            margin-bottom: var(--space-3);
            padding-bottom: var(--space-3);
            border-bottom: 2px solid var(--color-border-subtle);
        }

        .section-description {
            font-size: var(--text-lg);
            color: var(--color-text-secondary);
            line-height: var(--leading-relaxed);
            margin-bottom: var(--space-3);
        }

        /* Cards Grid */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: var(--space-6);
            margin-bottom: var(--space-8);
        }
        /* Mobile Header */
        

        /* Mobile Menu Toggle */
        

        


        /* Theme Toggle */
        /* Sidebar Overlay */
        

        .theme-toggle {
            position: fixed;
            bottom: var(--space-6);
            right: var(--space-6);
            z-index: 1000;
        }

                
    </style>
</head>
<body>
    <!-- Mobile Header -->
    
    </div>

    <!-- Sidebar Overlay -->
    


    <!-- Sidebar Navigation -->
    

    <!-- Main Content -->
    <main class="main-content">
        <!-- Contributing -->
        <section class="section" id="contributing">
            <h2 class="section-title">Contributing</h2>
            <p class="section-description">
                Help us make Aural UI even better! We welcome contributions from the community.
            </p>

            <div class="cards-grid">
                <div class="card" style="padding: var(--space-6);">
                    <div style="display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-3);">
                        <i data-lucide="book-open" style="width: 24px; height: 24px; color: var(--color-primary);"></i>
                        <h3 style="margin: 0; font-size: var(--text-lg);">Contributing Guide</h3>
                    </div>
                    <p style="color: var(--color-text-secondary); margin-bottom: var(--space-4);">
                        Read our comprehensive guide on how to contribute, including setup instructions,
                        code standards, and PR guidelines.
                    </p>
                    <a href="https://github.com/ferology/aural-ui/blob/main/CONTRIBUTING.md"
                       target="_blank"
                       class="btn btn-primary btn-sm"
                       style="text-decoration: none;">
                        <i data-lucide="external-link" style="width: 16px; height: 10px;"></i>
                        View Guide
                    </a>
                </div>

                <div class="card" style="padding: var(--space-6);">
                    <div style="display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-3);">
                        <i data-lucide="github" style="width: 24px; height: 24px; color: var(--color-primary);"></i>
                        <h3 style="margin: 0; font-size: var(--text-lg);">GitHub Repository</h3>
                    </div>
                    <p style="color: var(--color-text-secondary); margin-bottom: var(--space-4);">
                        Star the repository, open issues, submit pull requests, or join discussions.
                        All contributions are welcome!
                    </p>
                    <a href="https://github.com/ferology/aural-ui"
                       target="_blank"
                       class="btn btn-secondary btn-sm"
                       style="text-decoration: none;">
                        <i data-lucide="star" style="width: 16px; height: 10px;"></i>
                        View on GitHub
                    </a>
                </div>

                <div class="card" style="padding: var(--space-6);">
                    <div style="display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-3);">
                        <i data-lucide="message-circle" style="width: 24px; height: 24px; color: var(--color-primary);"></i>
                        <h3 style="margin: 0; font-size: var(--text-lg);">Report Issues</h3>
                    </div>
                    <p style="color: var(--color-text-secondary); margin-bottom: var(--space-4);">
                        Found a bug or have a feature request? Open an issue on GitHub with detailed
                        information and we'll look into it.
                    </p>
                    <a href="https://github.com/ferology/aural-ui/issues"
                       target="_blank"
                       class="btn btn-secondary btn-sm"
                       style="text-decoration: none;">
                        <i data-lucide="alert-circle" style="width: 16px; height: 10px;"></i>
                        Report Issue
                    </a>
                </div>
            </div>
        </section>
    </main>

    <!-- Theme Toggle -->
    <div class="theme-toggle">
        <button class="btn btn-secondary" onclick="toggleTheme()">
            <i data-lucide="sun"></i>
            <span class="theme-toggle-text">Toggle Theme</span>
        </button>
    </div>

    <!-- Aural UI JavaScript -->
    <script src="./aural-ui.js?v=9"></script>

    <!-- Initialize Lucide Icons -->
    <script>
        lucide.createIcons();
    </script>

    <!-- Navigation and Theme Scripts -->
    <script>
        // Toggle collapsible navigation sections
        
        // Mobile menu toggle
        
        }


        // Theme toggle functionality
        function toggleTheme() {
            const themeLink = document.getElementById('theme-link');
            const currentTheme = themeLink.getAttribute('href');

            if (currentTheme.includes('dark')) {
                themeLink.setAttribute('href', './light.css');
                localStorage.setItem('aural-theme', 'light');
            } else {
                themeLink.setAttribute('href', './dark.css');
                localStorage.setItem('aural-theme', 'dark');
            }
        }

        // Load saved theme on page load
        window.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('aural-theme') || 'dark';
            const themeLink = document.getElementById('theme-link');
            themeLink.setAttribute('href', `./${savedTheme}.css`);
        });
    </script>

    <script>

        // Auto-scroll active nav item into view
        document.addEventListener('DOMContentLoaded', function() {
            const activeLink = document.querySelector('.nav-link.active');
            if (activeLink) {
                // Scroll the active item into view with some offset
                setTimeout(() => {
                    activeLink.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }, 100);
            }
        });

    </script>
    <!-- Unified Theme Sync -->
    <script>
    (function() {
        // Sync theme from localStorage
        const savedTheme = localStorage.getItem('theme') || 'dark';
        const themeLink = document.getElementById('theme-link');
        if (themeLink) {
            const isInComponents = window.location.pathname.includes('/components/');
            const relativePath = isInComponents ? '../' : './';
            themeLink.href = savedTheme === 'dark' ? relativePath + 'dark.css' : relativePath + 'light.css';
        }

        // Hide theme toggle in iframe (demo.html controls theme)
        try {
            if (window.self !== window.top) {
                const themeToggle = document.querySelector('.theme-toggle');
                if (themeToggle) {
                    themeToggle.style.display = 'none';
                }
            }
        } catch (e) {}
    })();
    </script>
</body>
</html>
