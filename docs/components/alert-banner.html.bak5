<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alert Banner - Aural UI</title>

    <!-- Aural UI CSS -->
    <link rel="stylesheet" href="../aural-ui.css?v=9">

    <!-- Theme CSS (will be set by JavaScript) -->
    <link rel="stylesheet" href="../dark.css" id="theme-link">

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        body {
            display: flex;
            margin: 0;
            padding: 0;
        }

        /* Sidebar Navigation */
        

        

        

        

        

        

        
        
        
        
        
        
        
        

        
            50% { transform: scaleY(1); opacity: 1; }
        }

        

        

        

        
            50% { transform: scale(1.3); opacity: 0.7; }
        }

        
            50% { transform: scale(0.9); opacity: 0.5; }
        }

        

        

        

        

        

        

        

        

        

        

        

        

        

        

        /* Main Content */
        .main-content {
            
            flex: 1;
            padding: var(--space-8);
            max-width: calc(1400px + 280px);
        }

        .page-header {
            margin-bottom: var(--space-8);
        }

        .page-title {
            font-size: var(--text-4xl);
            font-weight: var(--font-bold);
            color: var(--color-text-primary);
            margin-bottom: var(--space-4);
        }

        .page-description {
            font-size: var(--text-lg);
            color: var(--color-text-secondary);
            line-height: var(--leading-relaxed);
        }

        /* Example Container */
        .example-container {
            margin-bottom: var(--space-8);
        }

        .example-label {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: var(--tracking-wide);
            margin-bottom: var(--space-3);
        }

        .example-demo {
            padding: var(--space-6);
            background: var(--color-bg-secondary);
            border: 1px solid var(--color-border-subtle);
            border-radius: var(--radius-md);
            margin-bottom: var(--space-4);
        }

        /* Theme Toggle */
        /* Sidebar Overlay */
        

        .theme-toggle {
            position: fixed;
            bottom: var(--space-6);
            right: var(--space-6);
            z-index: 1000;
        }

        /* Mobile Header */
        

        /* Mobile Menu Toggle */
        

        

                /* Mobile Styles */
                

        

        
    </style>
</head>
<body>
    <!-- Mobile Header -->
    
    </div>

    <!-- Sidebar Overlay -->
    

    <!-- Mobile Overlay -->
    

    <!-- Theme Toggle -->
    <div class="theme-toggle">
        <button class="btn btn-secondary" onclick="toggleTheme()">
            <i data-lucide="sun"></i>
            <span class="theme-toggle-text">Toggle Theme</span>
        </button>
    </div>

    <!-- Sidebar Navigation -->
    

    <!-- Main Content -->
    <main class="main-content">
        <div class="page-header">
            <h1 class="page-title">Alert Banner</h1>
            <p class="page-description">
                Page-level alerts and notifications.
            </p>
        </div>

        
        <div class="example-container">
                <div class="example-label">Alert Variants</div>
                <div class="example-demo">
                    <div style="display: flex; flex-direction: column; gap: var(--space-4);">
                        <div class="aural-alert-banner aural-alert-banner--info">
                            <svg class="aural-alert-banner__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="10"/>
                                <line x1="12" y1="16" x2="12" y2="12"/>
                                <line x1="12" y1="8" x2="12.01" y2="8"/>
                            </svg>
                            <div class="aural-alert-banner__content">
                                <div class="aural-alert-banner__title">Info</div>
                                <div class="aural-alert-banner__message">This is an informational message.</div>
                            </div>
                        </div>

                        <div class="aural-alert-banner aural-alert-banner--success">
                            <svg class="aural-alert-banner__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <polyline points="20 6 9 17 4 12"/>
                            </svg>
                            <div class="aural-alert-banner__content">
                                <div class="aural-alert-banner__title">Success</div>
                                <div class="aural-alert-banner__message">Your changes have been saved successfully!</div>
                            </div>
                        </div>

                        <div class="aural-alert-banner aural-alert-banner--warning">
                            <svg class="aural-alert-banner__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
                                <line x1="12" y1="9" x2="12" y2="13"/>
                                <line x1="12" y1="17" x2="12.01" y2="17"/>
                            </svg>
                            <div class="aural-alert-banner__content">
                                <div class="aural-alert-banner__title">Warning</div>
                                <div class="aural-alert-banner__message">Please review your settings before proceeding.</div>
                            </div>
                        </div>

                        <div class="aural-alert-banner aural-alert-banner--error">
                            <svg class="aural-alert-banner__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="10"/>
                                <line x1="12" y1="8" x2="12" y2="12"/>
                                <line x1="12" y1="16" x2="12.01" y2="16"/>
                            </svg>
                            <div class="aural-alert-banner__content">
                                <div class="aural-alert-banner__title">Error</div>
                                <div class="aural-alert-banner__message">An error occurred while processing your request.</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="example-container">
                <div class="example-label">With Actions</div>
                <div class="example-demo">
                    <button class="btn btn-primary" onclick="Aural.showAlertBanner('Your session will expire in 5 minutes', 'warning', { title: 'Session Expiring', dismissible: true, fixed: 'top', actions: [{ label: 'Extend Session', primary: true, onClick: () => Aural.showToast('Session extended!', 'success') }, { label: 'Dismiss', onClick: () => {} }] })">
                        Show Alert with Actions
                    </button>
                </div>
            </div>
    
    </main>

    <!-- Aural UI JavaScript -->
    <script src="../aural-ui.js?v=9"></script>

    <!-- Navigation & Theme Toggle -->
    <script>
        

        

        

        // Theme Toggle with localStorage persistence
        function toggleTheme() {
            const themeLink = document.getElementById('theme-link');
            const currentTheme = themeLink.getAttribute('href');

            if (currentTheme.includes('dark')) {
                themeLink.setAttribute('href', '../light.css');
                localStorage.setItem('aural-theme', 'light');
            } else {
                themeLink.setAttribute('href', '../dark.css');
                localStorage.setItem('aural-theme', 'dark');
            }
        }

        // Load saved theme on page load
        window.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('aural-theme') || 'dark';
            const themeLink = document.getElementById('theme-link');
            themeLink.setAttribute('href', `../${savedTheme}.css`);

            // Close mobile menu when clicking navigation links
            const navLinks = document.querySelectorAll('.nav-link');
            navLinks.forEach(link => {
                link.addEventListener('click', () => {
                    // Only close on mobile
                    if (window.innerWidth <= 768) {
                    }
                });
            });
        });

        // Initialize Lucide Icons
        lucide.createIcons();

        // Aural UI Alert Banner & Toast System
        window.Aural = window.Aural || {};

        Aural.showAlertBanner = function(message, variant = 'info', options = {}) {
            const {
                title = variant.charAt(0).toUpperCase() + variant.slice(1),
                dismissible = true,
                fixed = 'top',
                actions = [],
                duration = null
            } = options;

            // Remove existing fixed banners
            const existingBanner = document.querySelector('.aural-alert-banner--fixed');
            if (existingBanner) {
                existingBanner.remove();
            }

            // Create banner element
            const banner = document.createElement('div');
            banner.className = `aural-alert-banner aural-alert-banner--${variant}`;
            if (fixed) {
                banner.classList.add('aural-alert-banner--fixed');
                // Account for sidebar width on desktop (280px)
                const leftOffset = window.innerWidth > 768 ? '280px' : '0';
                banner.style.cssText = `
                    position: fixed;
                    ${fixed}: 0;
                    left: ${leftOffset};
                    right: 0;
                    z-index: 9999;
                    animation: slideInFromTop 0.3s ease-out;
                `;
            }

            // Icon based on variant
            const icons = {
                info: '<circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/>',
                success: '<polyline points="20 6 9 17 4 12"/>',
                warning: '<path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>',
                error: '<circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>'
            };

            banner.innerHTML = `
                <svg class="aural-alert-banner__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    ${icons[variant] || icons.info}
                </svg>
                <div class="aural-alert-banner__content">
                    <div class="aural-alert-banner__title">${title}</div>
                    <div class="aural-alert-banner__message">${message}</div>
                    ${actions.length > 0 ? `
                        <div class="aural-alert-banner__actions" style="display: flex; gap: var(--space-2); margin-top: var(--space-2);">
                            ${actions.map(action => `
                                <button class="btn ${action.primary ? 'btn-primary' : 'btn-secondary'}" style="padding: var(--space-2) var(--space-3);">
                                    ${action.label}
                                </button>
                            `).join('')}
                        </div>
                    ` : ''}
                </div>
                ${dismissible ? `
                    <button class="aural-alert-banner__close" style="margin-left: auto; background: transparent; border: none; cursor: pointer; padding: var(--space-2);">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 20px; height: 20px;">
                            <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
                        </svg>
                    </button>
                ` : ''}
            `;

            document.body.appendChild(banner);

            // Add action handlers
            if (actions.length > 0) {
                const actionButtons = banner.querySelectorAll('.aural-alert-banner__actions button');
                actionButtons.forEach((btn, index) => {
                    btn.addEventListener('click', () => {
                        if (actions[index].onClick) {
                            actions[index].onClick();
                        }
                        banner.remove();
                    });
                });
            }

            // Add close handler
            if (dismissible) {
                const closeBtn = banner.querySelector('.aural-alert-banner__close');
                if (closeBtn) {
                    closeBtn.addEventListener('click', () => banner.remove());
                }
            }

            // Auto-dismiss if duration is set
            if (duration) {
                setTimeout(() => banner.remove(), duration);
            }

            return banner;
        };

        Aural.showToast = function(message, variant = 'info', duration = 3000) {
            const toast = document.createElement('div');
            toast.className = `aural-toast aural-toast--${variant}`;
            toast.style.cssText = `
                position: fixed;
                bottom: var(--space-6);
                right: var(--space-6);
                padding: var(--space-4);
                border-radius: var(--radius-md);
                background: var(--color-bg-secondary);
                border: 1px solid var(--color-border);
                box-shadow: var(--shadow-lg);
                z-index: 9999;
                animation: slideInFromBottom 0.3s ease-out;
            `;
            toast.textContent = message;
            document.body.appendChild(toast);

            setTimeout(() => {
                toast.style.animation = 'slideOutToBottom 0.3s ease-out';
                setTimeout(() => toast.remove(), 300);
            }, duration);

            return toast;
        };

        // Add CSS animations
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideInFromTop {
                from { transform: translateY(-100%); }
                to { transform: translateY(0); }
            }
            @keyframes slideInFromBottom {
                from { transform: translateY(100%); }
                to { transform: translateY(0); }
            }
            @keyframes slideOutToBottom {
                from { transform: translateY(0); }
                to { transform: translateY(100%); }
            }
        `;
        document.head.appendChild(style);
    </script>

    <script>

        // Auto-scroll active nav item into view
        document.addEventListener('DOMContentLoaded', function() {
            const activeLink = document.querySelector('.nav-link.active');
            if (activeLink) {
                // Scroll the active item into view with some offset
                setTimeout(() => {
                    activeLink.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }, 100);
            }
        });

    </script>
    <!-- Unified Theme Sync -->
    <script>
    (function() {
        // Sync theme from localStorage
        const savedTheme = localStorage.getItem('theme') || 'dark';
        const themeLink = document.getElementById('theme-link');
        if (themeLink) {
            const isInComponents = window.location.pathname.includes('/components/');
            const relativePath = isInComponents ? '../' : './';
            themeLink.href = savedTheme === 'dark' ? relativePath + 'dark.css' : relativePath + 'light.css';
        }

        // Hide theme toggle in iframe (demo.html controls theme)
        try {
            if (window.self !== window.top) {
                const themeToggle = document.querySelector('.theme-toggle');
                if (themeToggle) {
                    themeToggle.style.display = 'none';
                }
            }
        } catch (e) {}
    })();
    </script>
</body>
</html>
