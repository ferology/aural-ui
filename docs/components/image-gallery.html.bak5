<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Gallery - Aural UI</title>

    <!-- Aural UI CSS -->
    <link rel="stylesheet" href="../aural-ui.css?v=9">

    <!-- Theme CSS (will be set by JavaScript) -->
    <link rel="stylesheet" href="../dark.css" id="theme-link">

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        body {
            display: flex;
            margin: 0;
            padding: 0;
        }

        /* Sidebar Navigation */
        

        

        

        

        

        

        
        
        
        
        
        
        
        

        
            50% { transform: scaleY(1); opacity: 1; }
        }

        

        

        

        
            50% { transform: scale(1.3); opacity: 0.7; }
        }

        
            50% { transform: scale(0.9); opacity: 0.5; }
        }

        

        

        

        

        

        

        

        

        

        

        

        

        

        

        /* Main Content */
        .main-content {
            
            flex: 1;
            padding: var(--space-8);
            max-width: calc(1400px + 280px);
        }

        .page-header {
            margin-bottom: var(--space-8);
        }

        .page-title {
            font-size: var(--text-4xl);
            font-weight: var(--font-bold);
            color: var(--color-text-primary);
            margin-bottom: var(--space-4);
        }

        .page-description {
            font-size: var(--text-lg);
            color: var(--color-text-secondary);
            line-height: var(--leading-relaxed);
        }

        /* Example Container */
        .example-container {
            margin-bottom: var(--space-8);
        }

        .example-label {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: var(--tracking-wide);
            margin-bottom: var(--space-3);
        }

        .example-demo {
            padding: var(--space-6);
            background: var(--color-bg-secondary);
            border: 1px solid var(--color-border-subtle);
            border-radius: var(--radius-md);
            margin-bottom: var(--space-4);
        }

        /* Theme Toggle */
        /* Sidebar Overlay */
        

        .theme-toggle {
            position: fixed;
            bottom: var(--space-6);
            right: var(--space-6);
            z-index: 1000;
        }

        /* Mobile Header */
        

        /* Mobile Menu Toggle */
        

        

                /* Mobile Styles */
                

        

        
    </style>
</head>
<body>
    <!-- Mobile Header -->
    
    </div>

    <!-- Sidebar Overlay -->
    

    <!-- Mobile Overlay -->
    

    <!-- Theme Toggle -->
    <div class="theme-toggle">
        <button class="btn btn-secondary" onclick="toggleTheme()">
            <i data-lucide="sun"></i>
            <span class="theme-toggle-text">Toggle Theme</span>
        </button>
    </div>

    <!-- Sidebar Navigation -->
    

    <!-- Main Content -->
    <main class="main-content">
        <div class="page-header">
            <h1 class="page-title">Image Gallery</h1>
            <p class="page-description">
                Grid-based image gallery with lightbox.
            </p>
        </div>

        
        <h2>Image Gallery</h2>
        <p class="showcase-subtitle">Responsive grid gallery with lightbox modal for full-size viewing</p>

        <div class="example">
            <div class="example-label">3-Column Gallery</div>
            <div class="aural-gallery aural-gallery--cols-3" id="demo-gallery-1">
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=400&h=400&fit=crop" alt="Mountain landscape">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Mountain Vista</div>
                        <div class="aural-gallery__description">Breathtaking mountain landscape</div>
                    </div>
                </div>
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1518837695005-2083093ee35b?w=400&h=400&fit=crop" alt="Ocean sunset">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Ocean Sunset</div>
                        <div class="aural-gallery__description">Golden hour at the beach</div>
                    </div>
                </div>
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1511593358241-7eea1f3c84e5?w=400&h=400&fit=crop" alt="Forest trail">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Forest Path</div>
                        <div class="aural-gallery__description">Peaceful woodland trail</div>
                    </div>
                </div>
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1470071459604-3b5ec3a7fe05?w=400&h=400&fit=crop" alt="Northern lights">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Aurora Borealis</div>
                        <div class="aural-gallery__description">Northern lights display</div>
                    </div>
                </div>
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1469474968028-56623f02e42e?w=400&h=400&fit=crop" alt="Desert dunes">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Desert Dunes</div>
                        <div class="aural-gallery__description">Sand formations at sunrise</div>
                    </div>
                </div>
                <div class="aural-gallery__item" tabindex="0">
                    <img class="aural-gallery__image" src="https://images.unsplash.com/photo-1472214103451-9374bd1c798e?w=400&h=400&fit=crop" alt="Mountain lake">
                    <div class="aural-gallery__overlay">
                        <div class="aural-gallery__title">Alpine Lake</div>
                        <div class="aural-gallery__description">Crystal clear mountain water</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="example">
            <div class="example-label">Features</div>
            <ul style="list-style: none; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: var(--space-3);">
                <li style="display: flex; align-items: center; gap: var(--space-2);">
                    <svg style="width: 16px; height: 10px; color: var(--color-success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    <span>Lightbox modal</span>
                </li>
                <li style="display: flex; align-items: center; gap: var(--space-2);">
                    <svg style="width: 16px; height: 10px; color: var(--color-success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    <span>Keyboard navigation</span>
                </li>
                <li style="display: flex; align-items: center; gap: var(--space-2);">
                    <svg style="width: 16px; height: 10px; color: var(--color-success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    <span>Responsive grid</span>
                </li>
                <li style="display: flex; align-items: center; gap: var(--space-2);">
                    <svg style="width: 16px; height: 10px; color: var(--color-success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    <span>Image captions</span>
                </li>
            </ul>
        </div>
    
    </main>

    <!-- Aural UI JavaScript -->
    <script src="../aural-ui.js?v=9"></script>

    <!-- Navigation & Theme Toggle -->
    <script>
        

        

        

        // Theme Toggle with localStorage persistence
        function toggleTheme() {
            const themeLink = document.getElementById('theme-link');
            const currentTheme = themeLink.getAttribute('href');

            if (currentTheme.includes('dark')) {
                themeLink.setAttribute('href', '../light.css');
                localStorage.setItem('aural-theme', 'light');
            } else {
                themeLink.setAttribute('href', '../dark.css');
                localStorage.setItem('aural-theme', 'dark');
            }
        }

        // Load saved theme on page load
        window.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('aural-theme') || 'dark';
            const themeLink = document.getElementById('theme-link');
            themeLink.setAttribute('href', `../${savedTheme}.css`);

            // Close mobile menu when clicking navigation links
            const navLinks = document.querySelectorAll('.nav-link');
            navLinks.forEach(link => {
                link.addEventListener('click', () => {
                    // Only close on mobile
                    if (window.innerWidth <= 768) {
                    }
                });
            });
        });

        // Initialize Lucide Icons
        lucide.createIcons();
    </script>

    <script>

        // Auto-scroll active nav item into view
        document.addEventListener('DOMContentLoaded', function() {
            const activeLink = document.querySelector('.nav-link.active');
            if (activeLink) {
                // Scroll the active item into view with some offset
                setTimeout(() => {
                    activeLink.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }, 100);
            }
        });

    </script>
    <!-- Unified Theme Sync -->
    <script>
    (function() {
        // Sync theme from localStorage
        const savedTheme = localStorage.getItem('theme') || 'dark';
        const themeLink = document.getElementById('theme-link');
        if (themeLink) {
            const isInComponents = window.location.pathname.includes('/components/');
            const relativePath = isInComponents ? '../' : './';
            themeLink.href = savedTheme === 'dark' ? relativePath + 'dark.css' : relativePath + 'light.css';
        }

        // Hide theme toggle in iframe (demo.html controls theme)
        try {
            if (window.self !== window.top) {
                const themeToggle = document.querySelector('.theme-toggle');
                if (themeToggle) {
                    themeToggle.style.display = 'none';
                }
            }
        } catch (e) {}
    })();
    </script>
</body>
</html>
