<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stepper - Aural UI</title>

    <!-- Aural UI CSS -->
    <link rel="stylesheet" href="../aural-ui.css?v=9">

    <!-- Theme CSS (will be set by JavaScript) -->
    <link rel="stylesheet" href="../dark.css" id="theme-link">

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        body {
            display: flex;
            margin: 0;
            padding: 0;
        }

        /* Sidebar Navigation */
        

        

        

        

        

        

        
        
        
        
        
        
        
        

        
            50% { transform: scaleY(1); opacity: 1; }
        }

        

        

        

        
            50% { transform: scale(1.3); opacity: 0.7; }
        }

        
            50% { transform: scale(0.9); opacity: 0.5; }
        }

        

        

        

        

        

        

        

        

        

        

        

        

        

        

        /* Main Content */
        .main-content {
            
            flex: 1;
            padding: var(--space-8);
            max-width: calc(1400px + 280px);
        }

        .page-header {
            margin-bottom: var(--space-8);
        }

        .page-title {
            font-size: var(--text-4xl);
            font-weight: var(--font-bold);
            color: var(--color-text-primary);
            margin-bottom: var(--space-4);
        }

        .page-description {
            font-size: var(--text-lg);
            color: var(--color-text-secondary);
            line-height: var(--leading-relaxed);
        }

        /* Example Container */
        .example-container {
            margin-bottom: var(--space-8);
        }

        .example-label {
            font-size: var(--text-sm);
            font-weight: var(--font-semibold);
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: var(--tracking-wide);
            margin-bottom: var(--space-3);
        }

        .example-demo {
            padding: var(--space-6);
            background: var(--color-bg-secondary);
            border: 1px solid var(--color-border-subtle);
            border-radius: var(--radius-md);
            margin-bottom: var(--space-4);
        }

        /* Theme Toggle */
        /* Sidebar Overlay */
        

        .theme-toggle {
            position: fixed;
            bottom: var(--space-6);
            right: var(--space-6);
            z-index: 1000;
        }

        /* Mobile Header */
        

        /* Mobile Menu Toggle */
        

        

                /* Mobile Styles */
                

        

        
    </style>
</head>
<body>
    <!-- Mobile Header -->
    
    </div>

    <!-- Sidebar Overlay -->
    

    <!-- Mobile Overlay -->
    

    <!-- Theme Toggle -->
    <div class="theme-toggle">
        <button class="btn btn-secondary" onclick="toggleTheme()">
            <i data-lucide="sun"></i>
            <span class="theme-toggle-text">Toggle Theme</span>
        </button>
    </div>

    <!-- Sidebar Navigation -->
    

    <!-- Main Content -->
    <main class="main-content">
        <div class="page-header">
            <h1 class="page-title">Stepper</h1>
            <p class="page-description">
                Step-by-step navigation for multi-step processes.
            </p>
        </div>

        
        <h2>Stepper</h2>
            <p class="showcase-subtitle">Step indicator for multi-step processes</p>

            <div class="example">
                <div class="example-label">Horizontal Stepper</div>
                <div class="aural-stepper aural-stepper--horizontal" id="demo-stepper-1">
                    <div class="aural-step aural-step--completed">
                        <div class="aural-step__indicator" data-step="1">
                            <svg class="aural-step__indicator-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <polyline points="20 6 9 17 4 12"/>
                            </svg>
                        </div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Account</div>
                            <div class="aural-step__description">Create your account</div>
                        </div>
                    </div>
                    <div class="aural-step aural-step--active">
                        <div class="aural-step__indicator" data-step="2">2</div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Profile</div>
                            <div class="aural-step__description">Add your details</div>
                        </div>
                    </div>
                    <div class="aural-step">
                        <div class="aural-step__indicator" data-step="3">3</div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Preferences</div>
                            <div class="aural-step__description">Set your preferences</div>
                        </div>
                    </div>
                    <div class="aural-step">
                        <div class="aural-step__indicator" data-step="4">4</div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Complete</div>
                            <div class="aural-step__description">Review and finish</div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="example">
                <div class="example-label">Vertical Stepper</div>
                <div class="aural-stepper aural-stepper--vertical" id="demo-stepper-2" style="max-width: 400px;">
                    <div class="aural-step aural-step--completed">
                        <div class="aural-step__indicator" data-step="1">
                            <svg class="aural-step__indicator-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <polyline points="20 6 9 17 4 12"/>
                            </svg>
                        </div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Order Placed</div>
                            <div class="aural-step__description">Your order has been confirmed</div>
                        </div>
                    </div>
                    <div class="aural-step aural-step--active">
                        <div class="aural-step__indicator" data-step="2">2</div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Processing</div>
                            <div class="aural-step__description">We're preparing your order</div>
                        </div>
                    </div>
                    <div class="aural-step">
                        <div class="aural-step__indicator" data-step="3">3</div>
                        <div class="aural-step__connector"></div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Shipped</div>
                            <div class="aural-step__description">On its way to you</div>
                        </div>
                    </div>
                    <div class="aural-step">
                        <div class="aural-step__indicator" data-step="4">4</div>
                        <div class="aural-step__content">
                            <div class="aural-step__title">Delivered</div>
                            <div class="aural-step__description">Package delivered</div>
                        </div>
                    </div>
                </div>
            </div>
    
    </main>

    <!-- Aural UI JavaScript -->
    <script src="../aural-ui.js?v=9"></script>

    <!-- Navigation & Theme Toggle -->
    <script>
        

        

        

        // Theme Toggle with localStorage persistence
        function toggleTheme() {
            const themeLink = document.getElementById('theme-link');
            const currentTheme = themeLink.getAttribute('href');

            if (currentTheme.includes('dark')) {
                themeLink.setAttribute('href', '../light.css');
                localStorage.setItem('aural-theme', 'light');
            } else {
                themeLink.setAttribute('href', '../dark.css');
                localStorage.setItem('aural-theme', 'dark');
            }
        }

        // Load saved theme on page load
        window.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('aural-theme') || 'dark';
            const themeLink = document.getElementById('theme-link');
            themeLink.setAttribute('href', `../${savedTheme}.css`);

            // Close mobile menu when clicking navigation links
            const navLinks = document.querySelectorAll('.nav-link');
            navLinks.forEach(link => {
                link.addEventListener('click', () => {
                    // Only close on mobile
                    if (window.innerWidth <= 768) {
                    }
                });
            });
        });

        // Initialize Lucide Icons
        lucide.createIcons();
    </script>

    <script>

        // Auto-scroll active nav item into view
        document.addEventListener('DOMContentLoaded', function() {
            const activeLink = document.querySelector('.nav-link.active');
            if (activeLink) {
                // Scroll the active item into view with some offset
                setTimeout(() => {
                    activeLink.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }, 100);
            }
        });

    </script>
    <!-- Unified Theme Sync -->
    <script>
    (function() {
        // Sync theme from localStorage
        const savedTheme = localStorage.getItem('theme') || 'dark';
        const themeLink = document.getElementById('theme-link');
        if (themeLink) {
            const isInComponents = window.location.pathname.includes('/components/');
            const relativePath = isInComponents ? '../' : './';
            themeLink.href = savedTheme === 'dark' ? relativePath + 'dark.css' : relativePath + 'light.css';
        }

        // Hide theme toggle in iframe (demo.html controls theme)
        try {
            if (window.self !== window.top) {
                const themeToggle = document.querySelector('.theme-toggle');
                if (themeToggle) {
                    themeToggle.style.display = 'none';
                }
            }
        } catch (e) {}
    })();
    </script>
</body>
</html>
